# resources

Static assets: fonts, sounds, Piper voices, bundled hotspot seed dataset, icons. Large binaries (ML models, voices) are fetched at first run, not committed.

Delivered by: Phase 0 onward. See [LODESTAR_SSOT.md](../LODESTAR_SSOT.md) — no implementation exists here until its phase begins; LODESTAR never commits placeholder code.
