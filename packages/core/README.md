# @lodestar/core

Core service: chokidar journal watcher, JSONL tailer, typed event bus, domain state reducers, SQLite (better-sqlite3) persistence, IPC/WebSocket bridge.

Delivered by: Phases 0–1. See [LODESTAR_SSOT.md](../../LODESTAR_SSOT.md) — no implementation exists here until its phase begins; LODESTAR never commits placeholder code.
