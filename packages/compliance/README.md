# @lodestar/compliance

The compliance test suite: banned-pattern scans, AI-vendor dependency bans, egress allowlist + redirect/loopback-bypass tests, guardrail red-team tests, consent-default assertions. Runs uncached via turbo run compliance; failing compliance blocks every merge.

Delivered by: Phase 0 (Step 0.11), extended every phase. See [LODESTAR_SSOT.md](../../LODESTAR_SSOT.md) — no implementation exists here until its phase begins; LODESTAR never commits placeholder code.
