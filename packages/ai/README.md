# @lodestar/ai

Layer 2 — Commander's Assistant. Local Ollama client (GPU-pinned), tool/function-calling schemas that map to Layer-1 functions, debrief writer. Never does raw math; never calls the cloud.

Delivered by: Phase 5. See [LODESTAR_SSOT.md](../../LODESTAR_SSOT.md) — no implementation exists here until its phase begins; LODESTAR never commits placeholder code.
