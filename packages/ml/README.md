# @lodestar/ml

Layer 3 — local ML. Yield predictor, prospector worth-it classifier, Bayesian yield-calibration loop. Trained and inferred locally; data never leaves the machine.

Delivered by: Phase 6. See [LODESTAR_SSOT.md](../../LODESTAR_SSOT.md) — no implementation exists here until its phase begins; LODESTAR never commits placeholder code.
